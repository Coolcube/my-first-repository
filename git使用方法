下载安装git
git --version查看版本号
码云-设置-ssh公钥
git bash指定文件夹
ssh -keygen -t rsa -C "邮箱号"
cat ~/.ssh/id_rsa.pub
复制公钥 到码云 确定即可
复制ssh地址
git clone 地址即可下载文件到本地

上传：
git status
git add .
git commit -m 'project initialize'
git push
